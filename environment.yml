name: coconut_mapping
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  # --- Python ---
  - python=3.11

  # --- Core scientific ---
  - numpy
  - scipy
  - pandas
  - scikit-learn
  - scikit-image        # skimage — used by rasterio/torchvision deps on Windows

  # --- Geospatial ---
  - rasterio
  - geopandas
  - shapely
  - pyproj
  - fiona
  - gdal

  # --- Visualization ---
  - matplotlib
  - tqdm

  # --- Deep Learning ---
  - pytorch
  - torchvision
  - cpuonly             # remove this line if you have a CUDA GPU

  # --- Utilities ---
  - pip
  - pip:
      - planetary-computer
      - pystac-client
      - tenacity
      - requests
