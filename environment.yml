name: coconut_mapping
channels:
  - conda-forge
  - pytorch
dependencies:
  # --- Python ---
  - python=3.11

  # --- Core scientific ---
  - numpy
  - scipy
  - pandas
  - scikit-learn

  # --- Geospatial ---
  - rasterio
  - geopandas
  - shapely
  - pyproj
  - fiona
  - gdal

  # --- Visualization ---
  - matplotlib
  - tqdm

  # --- Deep Learning ---
  - pytorch
  - torchvision

  # --- Utilities ---
  - pip
  - pip:
      - planetary-computer
      - pystac-client
      - tenacity
      - requests
